# 다시, 그날 — 서비스 및 기능 정리

## 1. 서비스 소개

`다시, 그날`은 사진과 짧은 코멘트를 바로 다시 보여주는 기록 앱이 아닙니다. 일정 시간이 지난 뒤 사진을 숨긴 채 먼저 기억을 떠올리게 하고, 원본을 본 뒤 새롭게 떠오른 내용을 더해 한 장의 추억 카드로 완성하는 회상 다이어리입니다.

서비스는 기억의 정확도를 채점하지 않습니다. AI는 정답을 판정하는 대신 회상을 돕는 질문을 만들고, 여러 차례 떠오른 기억을 자연스러운 이야기로 연결합니다.

```text
기록 저장
→ 일정 시간이 지난 뒤 원본 없이 회상
→ 당시 기록 확인
→ 새롭게 떠오른 기억 추가
→ 하나의 추억으로 정리
```

핵심 목표는 기억력 시험이 아니라, 일상의 경험을 다시 떠올리고 의미 있는 추억으로 남길 기회를 제공하는 것입니다.

## 2. 전체 사용자 흐름

### 2.1 기억 등록

사용자는 짧은 코멘트, 기억 날짜, 구체적인 장소, 사진 한 장, 사진 속 글자 활용 여부와 회상 가능일을 입력합니다. 사진과 장소는 선택 사항입니다.

`발표용 예시 채우기` 버튼을 누르면 데모에 적합한 코멘트와 장소가 자동 입력됩니다. 사진은 발표자가 준비한 티켓이나 영수증 등을 선택할 수 있습니다.

### 2.2 AI 기억 분석

등록이 끝나면 다음 순서로 처리합니다.

1. 글자가 있는 사진에 Document Parse OCR 적용
2. Information Extract로 사람·장소·활동·분위기 구조화
3. Solar LLM으로 제목·요약·회상 단서 생성
4. 구체적인 장소를 동네 공유용 표준 지역으로 추천

화면은 `사진 속 글자를 확인하고 있어요`, `기억의 장소와 인물을 정리하고 있어요`, `회상에 필요한 질문 단서를 만들고 있어요`처럼 현재 처리 단계를 알려줍니다.

AI 분석이 실패해도 원본 기억은 먼저 저장됩니다. 사용자는 `분석 다시 시도` 버튼으로 AI 처리만 다시 실행할 수 있습니다.

### 2.3 장소 추천과 검색

개인 기록의 구체적인 장소와 동네 카드의 표준 지역을 분리합니다.

```text
개인 기록: 광안리 해수욕장 앞 작은 카페
표준 지역: 광안리
```

별칭도 표준 지역으로 연결합니다.

```text
자갈치시장·국제시장·BIFF광장 → 남포동
전포동·전포카페거리 → 전포
태종대·흰여울문화마을 → 영도
미포·청사포·달맞이길 → 해운대
```

AI 추천은 자동 확정되지 않습니다. 사용자가 검색 가능한 지역 입력에서 결과를 확인하고 최종 확정해야 합니다. 추천에 실패해도 개인 기억과 회상 기능은 정상적으로 사용할 수 있습니다.

### 2.4 회상 가능일

회상 일정은 알림 발송 시간이 아니라 해당 기억이 `오늘의 회상` 화면에 나타나기 시작하는 시점입니다.

```text
1차 회상: 등록일 기준 7일 뒤
2차 회상: 등록일 기준 30일 뒤
```

사용자가 직접 바꿀 때 1차 회상은 0~30일, 2차 회상은 0~365일 범위에서 설정할 수 있습니다. 두 번째 회상 시점은 첫 번째보다 빠르게 지정할 수 없습니다.

과거의 기억을 오늘 등록해도 등록 시점을 기준으로 회상 기회를 제공합니다. 해커톤 데모에서는 `DEMO_MODE`로 하루를 몇 초로 압축할 수 있습니다.

현재 MVP는 서버 푸시·이메일을 보내지 않습니다. 앱이 열려 있을 때 회상 가능 목록과 개수 배지를 확인하는 구조입니다.

### 2.5 오늘의 회상

같은 날짜에 기록이 여러 개 있을 수 있으므로 날짜만 보여주지 않습니다. 같은 날의 몇 번째 기억인지, 넓은 지역, 사람·장소·활동·분위기 단서와 회상 차수를 함께 표시합니다. 이 정보는 원본 내용을 직접 노출하지 않습니다.

회상을 시작하면 Solar가 개방형 질문을 생성합니다. 답변을 저장하기 전에는 원본을 볼 수 없습니다. 작성 중인 답변은 사용자와 회상 세션별로 브라우저에 자동 임시저장되며, 화면을 벗어났다가 돌아오면 복원됩니다.

`아직 잘 기억나지 않아요`를 선택해도 실패로 처리하지 않습니다.

### 2.6 원본 공개와 추가 회상

첫 답변을 저장하면 원본 공개 버튼이 활성화됩니다. 원본 사진·코멘트·날짜·구체적인 장소를 확인한 뒤 새롭게 떠오른 장면·대화·감정을 추가할 수 있습니다.

### 2.7 추억 카드

원본 기억 하나당 추억 카드는 한 장만 유지합니다.

```text
1차 회상 → 추억 카드 최초 생성
2차 회상 → 새 카드를 만들지 않고 기존 카드 갱신
```

카드에는 AI 제목, 회상 전후를 연결한 이야기, 원본 사진, 장소, 1·2차 회상 타임라인이 들어갑니다. 2차 회상이 반영되면 새롭게 추가된 기억 조각 수를 카드 상단에서 강조합니다.

## 3. 동네 추억 카드

### 3.1 개인정보 보호

동네 공유의 기본값은 비공유입니다. 사용자가 동의해도 다음 정보는 공유하지 않습니다.

- 원본 사진과 코멘트
- 사용자 ID
- 사람 이름·별명
- 학교·단체·모임명
- 구체적인 이동 경로와 정확한 시각
- 개인을 특정할 수 있는 희귀 사건·영화명·메뉴명

Solar는 개인적인 표현을 `문화 활동`, `식사`, `산책`, `사람들과의 만남`처럼 넓은 범주로 일반화합니다.

### 3.2 공유 미리보기

공유 버튼을 눌러도 바로 저장하지 않습니다.

```text
개인 회상
→ Solar 비식별화
→ 실제 저장 예정 문장 미리보기
→ 사용자 최종 확인
→ 동네 조각 저장
```

미리보기에는 10분 동안 유효한 서버 서명을 붙입니다. 사용자가 확인한 문장이 전송 중 바뀌거나 조작되면 저장을 거부합니다.

1차 비식별 결과에 개인정보 후보가 남거나 결과가 비어 있으면 더 넓은 범주로 자동 재시도합니다. 재시도도 통과하지 못하면 원문을 복사하지 않는 제한된 범주형 문장(산책, 문화 활동, 식사와 휴식, 장소의 분위기 등)으로 대체합니다. 따라서 사용자가 개인 기록을 다시 고쳐 쓰지 않아도 안전한 공유 미리보기까지 이어집니다.

### 3.3 공유 상태

개인 카드에서 다음 상태를 확인할 수 있습니다.

- 공유 전
- 비식별 조각 공유됨·동네 카드 생성 대기
- 동네 카드에 반영됨

### 3.4 동네 카드 생성

같은 표준 지역에 서로 다른 사용자 3명의 조각이 모이면 첫 동네 카드를 생성할 수 있습니다. 장소마다 공개 카드는 한 장만 유지되며, 이후에는 이전 발행에 참여하지 않은 새 사용자 3명이 모일 때 같은 카드를 새 버전으로 갱신합니다.

```text
최초 3명 → 동네 카드 v1 생성
새 사용자 1~2명 → 기존 카드 유지
새 사용자 3명 → 같은 카드 ID로 v2 갱신
```

레코드 수가 아니라 서로 다른 사용자 수로 계산합니다. 같은 사용자가 같은 장소에 여러 추억 카드를 공유해도 한 명으로만 집계하고, 해당 사용자의 여러 조각을 먼저 하나로 묶어 다른 참여자와 같은 가중치로 AI에 전달합니다.

여러 사람에게 공통으로 나타난 분위기·감각·활동만 공개하며 개인별 이야기를 이어 붙이지 않습니다. 공유 조각을 만들 때 Solar가 욕설·비속어·혐오·모욕 표현을 중립적으로 일반화하고, 최종 동네 카드 저장 직전에 백엔드가 금칙 표현을 다시 강제 치환합니다.

### 3.5 부산 동네 추억 지도

Leaflet 위에 통계청 기반 부산 16개 구·군 GeoJSON 경계를 표시하고, 50개 표준 지역의 대표 좌표를 배치합니다. 부산이 섬처럼 보이지 않도록 인접한 울산·경남 행정구역은 옅은 중립색 육지 맥락으로만 표시하고, 부산 구·군만 고유 색상과 이름으로 강조합니다. 구·군에 커서를 올리면 해당 지역의 기억 현황이 나타납니다. 외부 지도 타일이나 별도 배경 그림을 사용하지 않으며 사용자의 현재 위치나 정확한 개인 장소 좌표도 수집하지 않습니다.

- 회색 마커: 아직 공유된 기억 없음
- 주황 마커: 공유 조각이 있지만 첫 카드 생성 대기
- 초록 마커: 동네 카드 발행 완료

마커를 누르면 참여 현황을 확인하고 해당 장소를 기존 장소 검색 입력과 연결할 수 있습니다. 지도는 `GET /archive/places/statuses` 한 번으로 모든 장소 상태를 받아오므로 장소별로 API를 반복 호출하지 않습니다.

### 3.6 AI 추억 카드 이미지

개인 추억 카드는 텍스트만으로도 완성되며 이미지 생성 실패가 카드 생성 자체를 막지 않습니다. 원본 사진이 있는 카드는 사진을 그대로 표시하고 별도 이미지 생성 기능을 제공하지 않습니다. 사진이 없는 카드만 카드 이야기와 새롭게 떠오른 조각을 바탕으로 이미지를 생성하며, 표현 방식은 사용자가 고르지 않아도 서비스가 기억의 맥락에 맞춰 자동 선택합니다.

생성 이미지는 원본과 분리된 비공개 Storage 경로에 저장되며, 화면에 AI 재해석 이미지임을 표시합니다. 사용자는 다시 생성하거나 생성 이미지만 삭제할 수 있고, 삭제 시 원본 사진과 텍스트 추억 카드는 유지됩니다.

## 4. 보관과 삭제 정책

### 카드 숨기기

- 목록에서 숨기지만 데이터는 유지
- `숨겨둔 카드`에서 확인하고 복구 가능
- 원본 기억과 회상 기록 유지

### 개인 기억 삭제

- 원본 사진·코멘트·OCR·AI 분석 삭제
- 연결된 회상과 개인 카드 삭제
- 발행 전 동네 조각 삭제
- 복구 불가

이미 동네 카드에 반영된 조각은 작성자 연결을 제거한 비식별 상태로만 유지합니다. 이미 발행된 공동체 기록이 개인 기억 삭제로 갑자기 바뀌지 않도록 하기 위한 정책이며, 해당 장소의 공개 카드 내용·기여자 수·버전도 삭제 직후에는 변하지 않습니다.

## 5. 사용자와 인증

현재 사용자 입력은 해커톤 시연용 `사용자 로그인` 기능입니다. 아이디를 입력하고 로그인 버튼을 누르면 그 아이디가 브라우저 `localStorage`에 저장되어 새로고침해도 유지되고, 로그아웃 전까지 입력창이 잠깁니다. 다른 아이디로 로그인하면 다른 사용자처럼 별도 데이터를 사용할 수 있지만, 비밀번호 확인 등 실제 로그인이나 신원 확인은 아닙니다. 관리자로 로그인한 동안에는 이 로그인 UI 자체가 숨겨지고 관리자 로그아웃 버튼만 보입니다.

백엔드는 데모·헤더·Supabase 인증 모드를 지원하지만 현재 데모 UI에는 Supabase 로그인 화면이 없습니다. 실제 배포 전에는 Supabase Auth 로그인과 사용자 토큰 전송을 연결해야 합니다.

## 6. 사용 기술

- FastAPI: API, 검증, 회상 일정, 소유권 확인
- Supabase PostgreSQL: 기억·회상·카드·동네 조각 저장
- Supabase Storage: 비공개 원본 사진 저장
- Document Parse: 사진 속 글자 추출
- Information Extract: 사실 기반 필드 구조화
- Solar LLM: 제목·요약·질문·카드·비식별화·동네 이야기
- Local Storage: 작성 중 회상 임시저장

## 7. 발표용 권장 시나리오

1. `발표용 예시 채우기`로 광안리 기억 입력
2. 글자가 있는 티켓·영수증 사진과 OCR 선택
3. AI 처리 단계와 광안리 추천 확인
4. 데모 압축 일정으로 오늘의 회상에 기억 등장
5. 원본 없이 답변하고 저장
6. 원본 공개 후 새 기억 추가
7. 추억 카드와 회상 타임라인 확인
8. 동네 공유 선택
9. 비식별 미리보기 확인 후 공유
10. 기여 상태와 동네 카드 확인

실제 Upstage 연동 검증:

```env
AI_MODE=upstage
AI_FALLBACK_TO_MOCK=false
```

발표 중 네트워크 장애 대비 백업:

```env
AI_MODE=auto
AI_FALLBACK_TO_MOCK=true
```

백업 모드에서는 Upstage 실패 시 mock 결과를 사용할 수 있으므로 실제 연동 검증에는 사용하지 않습니다.

## 8. 실행과 테스트

```bash
source venv/bin/activate
python run.py
```

```text
데모 화면: http://127.0.0.1:8000/demo/
API 문서:  http://127.0.0.1:8000/docs
상태 확인: http://127.0.0.1:8000/health
```

자동 테스트:

```bash
python -m pytest -q
```

과거 버전의 중복 카드 점검:

```bash
python scripts/audit_duplicate_cards.py
```

점검 결과를 확인한 뒤 동네 공유 이력이 없는 안전한 중복만 정리할 수 있습니다.

```bash
python scripts/audit_duplicate_cards.py --fix
```

공유 이력이 있는 카드는 자동 삭제하지 않습니다.

## 9. 주요 환경변수

```env
DATABASE_URL=Supabase_PostgreSQL_연결_주소
STORAGE_BACKEND=supabase
SUPABASE_URL=Supabase_프로젝트_URL
SUPABASE_SERVICE_ROLE_KEY=서버용_비밀키
SUPABASE_STORAGE_BUCKET=memory-images
AI_MODE=upstage
UPSTAGE_API_KEY=Upstage_API_키
DEMO_MODE=true
DEMO_DAY_SECONDS=2
FIRST_RECALL_DAYS=7
SECOND_RECALL_DAYS=30
PLACE_TAGS=["광안리","해운대","센텀",...]
SHARE_PREVIEW_SECRET=충분히_길고_무작위인_비밀값
```

비밀값이 들어간 `.env`는 GitHub에 올리지 않습니다.

## 10. 현재 구현 범위와 향후 확장

현재 구현됨:

- 기억 등록부터 2차 회상·카드 갱신까지 전체 흐름
- Supabase DB·Storage와 Upstage 3종 기능
- 장소 별칭과 검색형 표준 지역 선택
- 회상 자동 임시저장
- 동네 공유 미리보기·서명 검증·진행 상태
- 보관·삭제 정책과 중복 카드 점검
- 데모 압축 일정과 발표용 예시

배포 단계에서 추가할 기능:

- Supabase Auth 회원가입·로그인 UI
- 앱이 닫혀 있어도 도착하는 이메일·웹 푸시·모바일 알림
- 비동기 작업 큐와 AI 처리 상태 실시간 갱신
- 운영 오류·호출량 모니터링
- 개인정보 다운로드·전체 계정 삭제 화면

이 기능들은 외부 인증·알림 발송 자원과 운영 설정이 필요하므로 현재 해커톤 데모와 구분해 확장 로드맵으로 관리합니다.
